# Aqu� va la configuraci�n general del proyecto: conexi�n a base de datos, rutas de archivos, par�metros del sistema y valores por defecto.

#Nota: Por simplicidad inicial, la conexion local a PostgreSQL se configuro directamente en application.properties.
#En una version  posterior se pueden mover las credenciales a variables de entorno o a un archivo local ignorado por Git

db.url = jdbc:postgresql://localhost:5433/lsi_documentbase
db.user = postgres
db.password = Miami123